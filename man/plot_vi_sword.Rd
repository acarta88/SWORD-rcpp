% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{plot_vi_sword}
\alias{plot_vi_sword}
\title{Variable importance barplot for a SWORD forest}
\usage{
plot_vi_sword(
  forest,
  top_n = 20L,
  col = "#3498db",
  raw = FALSE,
  main = NULL,
  ...
)
}
\arguments{
\item{forest}{\code{sword_flat} object from \code{\link{SWORD}}.}

\item{top_n}{maximum number of variables to display (default 20).}

\item{col}{bar fill colour (default \code{"#3498db"}).}

\item{raw}{logical; if \code{TRUE} show importances at the dummy-column
level instead of the original variable level (default \code{FALSE}).}

\item{main}{plot title; \code{NULL} generates a default title.}

\item{...}{further arguments passed to \code{barplot}.}
}
\value{
Invisibly returns the named importance vector (full length, before
  truncation to \code{top_n}).
}
\description{
Calls \code{\link{VI_SWORD}} and draws a horizontal barplot of
deviance-reduction importance scores, sorted descending.
}
\examples{
\donttest{
set.seed(1)
X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                dimnames = list(NULL, paste0("x", 1:5))))
y <- X$x1 * 2 - X$x2 + rnorm(150)
forest <- SWORD(X, y, m = 10, oob = FALSE, verbose = FALSE)
plot_vi_sword(forest)
}
}
\seealso{
\code{\link{VI_SWORD}}, \code{\link{plot.sword_flat}}
}
