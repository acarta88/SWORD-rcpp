% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{SWORD}
\alias{SWORD}
\alias{print.sword_flat}
\alias{summary.sword_flat}
\title{Fit a SWORD forest}
\usage{
SWORD(
  Covariates,
  y = NULL,
  data = NULL,
  nmin = 5,
  minleaf = 2,
  cp = 0,
  n_topCor = 2,
  m = 10,
  rf_var = NULL,
  rand_ntopcor = TRUE,
  relation = "Pearson",
  Weight_Scheme = "scale",
  type_of_svm = "C-classification",
  cost_C = 1,
  cost_nu = 0.5,
  tolerance = 0.001,
  seed_BS = 25,
  parallel = FALSE,
  chunk = FALSE,
  n_chunks = NULL,
  n_workers = 1,
  OOB = TRUE,
  verbose = FALSE,
  timeout_tree = 300
)

\method{print}{sword_flat}(x, ...)

\method{summary}{sword_flat}(object, ...)
}
\arguments{
\item{Covariates}{data.frame of predictors, or a \code{formula} for the
formula interface.}

\item{y}{numeric response vector.}

\item{data}{data.frame; required when \code{Covariates} is a formula.}

\item{nmin}{minimum node size (default 5).}

\item{minleaf}{minimum leaf size (default 2).}

\item{cp}{complexity parameter (default 0). Unlike \code{\link{TORS}},
forests use \code{cp = 0} (full trees) by design: bootstrap sampling provides
regularisation and unpruned trees maximise diversity.}

\item{n_topCor}{top-correlated features per split (default 2).}

\item{m}{number of trees (default 10).}

\item{rf_var}{features sub-sampled per split (default ncol(Covariates)).}

\item{rand_ntopcor}{randomly draw top-feature cardinality (default TRUE).}

\item{relation}{\code{"Pearson"} or \code{"MI"}.}

\item{Weight_Scheme}{\code{"scale"} or \code{"robust"}.}

\item{type_of_svm}{SVM type: \code{"C-classification"} uses \code{cost_C};
\code{"nu-classification"} uses \code{cost_nu} (default \code{"C-classification"}).}

\item{cost_C}{SVM cost parameter C; used when
\code{type_of_svm = "C-classification"} (default 1).}

\item{cost_nu}{nu parameter; used when
\code{type_of_svm = "nu-classification"} (default 0.5).}

\item{tolerance}{convergence tolerance for the C++ solver (default 0.001).}

\item{seed_BS}{integer seed offset for bootstrap samples (default 25).}

\item{parallel}{if TRUE, fit trees in parallel via \code{furrr::future_map}
(default FALSE). Set a \code{future} plan before calling.}

\item{chunk}{if TRUE, process trees in chunks (default FALSE).}

\item{n_chunks}{number of chunks; NULL = ceiling(m / n_workers) (default NULL).}

\item{n_workers}{number of workers used only to derive the default
\code{n_chunks} when \code{chunk = TRUE} and \code{n_chunks = NULL}
(default 1). The actual parallelism is controlled by the \code{future}
plan set by the caller.}

\item{OOB}{if TRUE, compute OOB predictions and metrics (default TRUE).}

\item{verbose}{if TRUE, print tree index during sequential fitting (default FALSE).}

\item{timeout_tree}{max seconds per tree; NULL = no limit (default 300).}

\item{x}{\code{sword_flat} object (for \code{print}).}

\item{...}{ignored.}

\item{object}{\code{sword_flat} object (for \code{summary}).}
}
\value{
An object of class \code{"sword_flat"}.
}
\description{
Fits SWORD (Support vector machine Weighted Oblique Random Decision forests),
a random forest of TORS oblique regression trees. Each tree is trained on a
bootstrap sample; diversity is further increased by random predictor subspacing
and optional randomisation of the number of features per split. Out-of-bag
(OOB) predictions and metrics are computed when \code{OOB = TRUE}. Parallel
fitting is supported via \code{furrr} when the caller sets a \code{future}
plan before invoking \code{SWORD}.
}
\examples{
\donttest{
set.seed(1)
X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                dimnames = list(NULL, paste0("x", 1:5))))
y <- X$x1 * 2 - X$x2 + rnorm(150)
forest <- SWORD(X, y, m = 10, OOB = TRUE, verbose = FALSE)
print(forest)

# formula interface
forest2 <- SWORD(y ~ ., data = cbind(X, y = y), m = 10, verbose = FALSE)
}
}
\references{
Carta, A. and Frigau, L. (2025). Tree oblique for regression with weighted
support vector machine. \emph{Computational Statistics}, \bold{40}, 5257--5291.
\doi{10.1007/s00180-025-01647-w}

Yang, X., Song, Q. and Cao, A. (2005). Weighted support vector machine for
data classification. In \emph{Proceedings of the 2005 IEEE International
Joint Conference on Neural Networks (IJCNN)}.
\doi{10.1109/IJCNN.2005.1555965}

Xu, T. et al. (2024). \emph{WeightSVM: Subject/Instance Weighted Support
Vector Machines}. R package version 1.7-16.
\doi{10.32614/CRAN.package.WeightSVM}
}
\seealso{
\code{\link{TORS}}, \code{\link{predict.sword_flat}}, \code{\link{VI_SWORD}}
}
