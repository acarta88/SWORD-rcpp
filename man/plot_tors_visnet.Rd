% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{plot_tors_visnet}
\alias{plot_tors_visnet}
\title{Interactive zoomable visualisation of a TORS tree}
\usage{
plot_tors_visnet(tree, use_scaled = TRUE, top_k = 4L, digits = 3L)
}
\arguments{
\item{tree}{\code{tors_flat} object from \code{\link{TORS}}.}

\item{use_scaled}{logical; use scaled-space coefficients for labels
(default \code{TRUE}).}

\item{top_k}{maximum number of equation terms shown on each node
(default 4). Full equation is always in the hover tooltip.}

\item{digits}{decimal places for coefficients (default 3).}
}
\value{
A \code{visNetwork} HTML widget (rendered as a side-effect in
  RStudio / a browser).
}
\description{
Renders an interactive HTML widget of the tree. Supports zoom (mouse wheel),
pan (click-drag), and hover tooltips with the full hyperplane equation.
Requires the \pkg{visNetwork} package.
}
\examples{
\donttest{
if (requireNamespace("visNetwork", quietly = TRUE)) {
  set.seed(1)
  X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                  dimnames = list(NULL, paste0("x", 1:5))))
  y <- X$x1 * 2 - X$x2 + rnorm(150)
  tree <- TORS(X, y, nmin = 10, cp = 0.01)
  plot_tors_visnet(tree)
}
}
}
\seealso{
\code{\link{TORS}}, \code{\link{plot_tors_text}}
}
