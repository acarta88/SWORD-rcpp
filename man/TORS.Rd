% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{TORS}
\alias{TORS}
\alias{print.tors_flat}
\alias{summary.tors_flat}
\title{Fit a TORS oblique regression tree}
\usage{
TORS(
  Covariates,
  y = NULL,
  data = NULL,
  nmin = 5,
  minleaf = round(nmin/3),
  cp = 0.01,
  n_perc = 1,
  n_topCor = 2,
  rf_var = NULL,
  rand_ntopcor = FALSE,
  relation = "Pearson",
  Weight_Scheme = "scale",
  type_of_svm = "C-classification",
  cost_C = 1,
  cost_nu = 0.5,
  tolerance = 0.001
)

\method{print}{tors_flat}(x, ...)

\method{summary}{tors_flat}(object, ...)
}
\arguments{
\item{Covariates}{data.frame of predictors (no response column), or a
\code{formula} for the formula interface.}

\item{y}{numeric response vector (length = nrow(Covariates)).}

\item{data}{data.frame; required when \code{Covariates} is a formula.}

\item{nmin}{minimum node size to attempt a split (default 5).}

\item{minleaf}{minimum leaf size after a split (default round(nmin/3)).}

\item{cp}{complexity parameter: minimum relative post-split deviance
(default 0.01; use 0 to grow the full tree).}

\item{n_perc}{number of candidate quantile thresholds per split (default 1).}

\item{n_topCor}{number of top-correlated features per split (default 2).}

\item{rf_var}{features randomly sub-sampled per split (default ncol(Covariates)).}

\item{rand_ntopcor}{if TRUE, randomly draw the cardinality of top features (default FALSE).}

\item{relation}{feature selection criterion: \code{"Pearson"} or \code{"MI"}.}

\item{Weight_Scheme}{observation weighting: \code{"scale"} or \code{"robust"}.}

\item{type_of_svm}{SVM type: \code{"C-classification"} uses the cost parameter
\code{cost_C}; \code{"nu-classification"} uses \code{cost_nu} (fraction of
margin errors/support vectors, in (0, 1]).}

\item{cost_C}{SVM cost parameter C; used when
\code{type_of_svm = "C-classification"} (default 1).}

\item{cost_nu}{nu parameter; used when
\code{type_of_svm = "nu-classification"} (default 0.5).}

\item{tolerance}{convergence tolerance for the C++ solver (default 0.001).}

\item{x}{\code{tors_flat} object (for \code{print}).}

\item{...}{ignored.}

\item{object}{\code{tors_flat} object (for \code{summary}).}
}
\value{
An object of class \code{"tors_flat"}.
}
\description{
Grows one Tree Oblique for Regression with weighted SVM (TORS). At each
internal node a Weighted-SVM classifier with a linear kernel defines the
oblique splitting hyperplane as a sparse linear combination of predictors.
The tree is stored in a flat (parallel vector) representation for memory
efficiency and fast prediction.
}
\examples{
set.seed(1)
X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                dimnames = list(NULL, paste0("x", 1:5))))
y <- X$x1 * 2 - X$x2 + rnorm(150)
tree <- TORS(X, y, nmin = 10, cp = 0.01)
print(tree)

# formula interface
tree2 <- TORS(y ~ ., data = cbind(X, y = y), nmin = 10, cp = 0.01)
}
\seealso{
\code{\link{SWORD}}, \code{\link{predict.tors_flat}}
}
