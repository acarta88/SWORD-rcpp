% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{plot_tors_text}
\alias{plot_tors_text}
\title{Indented text representation of a TORS tree}
\usage{
plot_tors_text(tree, use_scaled = FALSE, top_k = 3L, digits = 3L)
}
\arguments{
\item{tree}{\code{tors_flat} object from \code{\link{TORS}}.}

\item{use_scaled}{logical; use scaled-space coefficients (default \code{FALSE}).}

\item{top_k}{maximum number of terms per equation (default 3).}

\item{digits}{decimal places for coefficients (default 3).}
}
\value{
Invisibly returns \code{tree}.
}
\description{
Prints the tree node by node with an indented layout. Each internal node
shows the hyperplane equation; each leaf shows the predicted mean and
sample size.
}
\examples{
set.seed(1)
X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                dimnames = list(NULL, paste0("x", 1:5))))
y <- X$x1 * 2 - X$x2 + rnorm(150)
tree <- TORS(X, y, nmin = 10, cp = 0.01)
plot_tors_text(tree, top_k = 2)
}
\seealso{
\code{\link{TORS}}, \code{\link{plot_tors_visnet}}
}
