% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{wsvm_cd_cpp}
\alias{wsvm_cd_cpp}
\title{Weighted linear C-SVM via coordinate descent (bias augmentation)}
\usage{
wsvm_cd_cpp(x, y_bin, w_obs, C = 1, max_iter = 5000L, tol = 0.001)
}
\arguments{
\item{x}{Numeric matrix (n x p) of scaled features.}

\item{y_bin}{Integer vector of class labels in \{-1, +1\}.}

\item{w_obs}{Numeric vector of per-observation weights (positive).}

\item{C}{SVM cost parameter (default 1.0).}

\item{max_iter}{Maximum number of full cyclic passes (default 5000).}

\item{tol}{Convergence threshold: stop when max|delta_alpha| < tol (default 1e-3).}
}
\value{
A named list with elements \code{w} (primal weights, length p) and
  \code{b} (scalar bias); decision = \code{w^T x_scaled + b}.
}
\description{
Solves a weighted linear SVM in the primal via a cyclic coordinate-descent
algorithm with bias augmentation. Used internally by \code{\link{TORS}} and
\code{\link{SWORD}} at each tree node to find the optimal oblique splitting
hyperplane. Exposed for users who need direct access to the low-level solver.
}
