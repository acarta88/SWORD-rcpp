% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{pdp_sword}
\alias{pdp_sword}
\title{Partial dependence plot for a SWORD forest}
\usage{
pdp_sword(
  forest,
  X,
  var,
  n_grid = 50L,
  rug = TRUE,
  main = NULL,
  xlab = var,
  ylab = "Partial dependence",
  ...
)
}
\arguments{
\item{forest}{\code{sword_flat} object from \code{\link{SWORD}}.}

\item{X}{data.frame of predictor values (same columns used to fit the
forest; typically the training set).}

\item{var}{character string; name of the variable to profile.}

\item{n_grid}{number of equally-spaced grid points over the variable's
observed range (default 50).}

\item{rug}{logical; add a rug of observed values (default \code{TRUE}).}

\item{main}{plot title; \code{NULL} generates a default title.}

\item{xlab}{x-axis label (default: the variable name).}

\item{ylab}{y-axis label (default \code{"Partial dependence"}).}

\item{...}{further graphical arguments passed to \code{plot}.}
}
\value{
Invisibly returns a data.frame with columns \code{x} (grid values)
  and \code{yhat} (partial dependence values).
}
\description{
Computes the marginal effect of a single predictor on forest predictions by
fixing the variable to a grid of values and averaging predictions over the
full data matrix (Friedman 2001). A rug of observed values is added by
default.
}
\examples{
\donttest{
set.seed(1)
X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                dimnames = list(NULL, paste0("x", 1:5))))
y <- X$x1 * 2 - X$x2 + rnorm(150)
forest <- SWORD(X, y, m = 10, OOB = FALSE, verbose = FALSE)
pdp_sword(forest, X, "x1")
}
}
\seealso{
\code{\link{SWORD}}, \code{\link{plot.sword_flat}}
}
