% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SWORD.R
\name{plot.tors_flat}
\alias{plot.tors_flat}
\title{Plot a fitted TORS tree}
\usage{
\method{plot}{tors_flat}(x, use_scaled = TRUE, top_k = 4L, digits = 3L, ...)
}
\arguments{
\item{x}{\code{tors_flat} object from \code{\link{TORS}}.}

\item{use_scaled}{logical; use scaled-space coefficients (default \code{TRUE}).}

\item{top_k}{max equation terms shown per node (default 4).}

\item{digits}{decimal places for coefficients (default 3).}

\item{...}{ignored.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Dispatches to \code{\link{plot_tors_visnet}} when \pkg{visNetwork} is
installed; otherwise draws a base-R structure plot (node size proportional
to observation count).
}
\examples{
\donttest{
set.seed(1)
X <- data.frame(matrix(rnorm(150 * 5), 150, 5,
                dimnames = list(NULL, paste0("x", 1:5))))
y <- X$x1 * 2 - X$x2 + rnorm(150)
tree <- TORS(X, y, nmin = 10, cp = 0.01)
plot(tree)
}
}
\seealso{
\code{\link{plot_tors_visnet}}, \code{\link{plot_tors_text}}
}
